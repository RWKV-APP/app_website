# Local backend origin used by Next.js development rewrites.
# In local development the browser talks to http://localhost:3010,
# and Next.js proxies API requests to the backend origin below.
BACKEND_ORIGIN=http://localhost:3001
