---
description: Release notes 多语言同步与批量重命名流程（以 zh-Hans 为源，翻译到各语言后重命名文件）
globs: backend/data/release-notes/**/*.md
alwaysApply: false
---

# Release Notes 同步与重命名

当用户要求「同步 release notes」「做一遍 release notes 同步」「按 release notes 流程做一遍」或类似表述时，按以下流程执行。

## 1. 确定源文件与目标文件名

- **源文件**：以 `backend/data/release-notes/zh-Hans/` 下**用户正在编辑或指定的**那个 md 文件为唯一真相源（通常已包含最新版本的小节，如 4.2.6）。
- **目标文件名**：用户会说明新文件名（例如 `698-4.2.6.md`）。若未说明，则取当前文件名中的 build 号加 1、版本号取文档最顶部 `## x.y.z` 的版本，得到新文件名，例如 `697-4.2.5.md` → `698-4.2.6.md`。

## 2. 同步内容到所有语言

- 在 `backend/data/release-notes/` 下对每个语言目录执行：
  - **zh-Hant**：简体转繁体，保持 [新增/优化/修复] 等标签风格。
  - **en**：译为英文，使用 [New] / [Improved] / [Fix]。
  - **ja**：日文，使用 [新規] / [改善] / [修正]。
  - **ru**：俄文，使用 [Новое] / [Улучшено] / [Исправлено]。
  - **ko**：韩文，使用 [신규] / [개선] / [수정]。
- 只同步**源文件中已有、而该语言文件尚未包含**的版本小节（通常是文档最顶部的那个新版本）；若某语言文件已有完整内容，则仅补全缺失的顶部版本并翻译。
- 翻译时保持与同文件内已有条目风格一致（术语、句式、括号用法等）。

## 3. 批量重命名

- 在所有语言目录下，将**当前**的 release note 文件名（如 `697-4.2.5.md`）改为**新**文件名（如 `698-4.2.6.md`）。
- 操作方式：先写入新文件名对应内容（内容已在步骤 2 中更新），再删除旧文件名对应文件。
- 确保 zh-Hans、zh-Hant、en、ja、ru、ko 六个语言目录都完成重命名，且新文件内容一致（仅语言不同）。

## 4. 约定与注意

- 语言目录列表以当前仓库下 `backend/data/release-notes/` 实际存在的为准（当前为 zh-Hans, zh-Hant, en, ja, ru, ko）。
- 源文件中的笔误（如「修修复了」）在同步时顺带改为正确表述（如「修复了」）。
- 完成后简要列出：已同步的版本/条目、各语言新文件名及路径。
