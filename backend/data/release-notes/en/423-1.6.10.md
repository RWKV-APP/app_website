- Clicking the new conversation button in the top right corner now directly creates a new conversation
- Optimized sorting logic for different models on the model selection panel, models are now sorted in descending order by size, models using npu/gpu are now ranked first
- Fine-tuned different rendering logic for "fast thinking" in Chinese and English modes
- Optimized side swipe menu rendering logic
- Added macOS support
- Added Windows support
